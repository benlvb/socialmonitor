node_modules/
dist/
.next/
.vercel/
.turbo/
coverage/
*.tsbuildinfo
.DS_Store
.env
.env.*
!.env.example
supabase/.temp/
.claude/settings.local.json

# Generated by `next dev` / `next build`; it references ./.next/types/* which is
# ignored above, and it churns between the dev and build variants. Regenerated by
# either command, and `tsc --noEmit` passes without it (verified on a fresh tree).
apps/web/next-env.d.ts

# Per-directory agent pointers. The ROOT CLAUDE.md is the repo's own guidance and
# stays tracked; nested ones are local scaffolding.
apps/*/CLAUDE.md
packages/*/CLAUDE.md
